extends ../layout

block content
  .grid
    form(action='/admin/remove-refreshpath', method='POST')
      input(type='hidden', name='_csrf', value=_csrf)
      input(type='hidden' name='id' value=refreshpath.id)
      button.col-md-7.btn.btn-danger(type='submit')
        i.fas.fa-trash-alt
        | Remove #{refreshpath.name} ? Are you sure ?  
